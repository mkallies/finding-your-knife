import styled from 'styled-components'
import {
  space,
  width,
  color,
  height,
  border,
  borderColor,
  borderRadius,
} from 'styled-system'

const Box = styled.div`
  ${space} 
  ${width} 
  ${color}
  ${height}
  ${border}
  ${borderColor}
  ${borderRadius}
`

export default Box
